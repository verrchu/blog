// The post's content lives in the sibling `examples/` directory.
//
//   examples/ext-api.rs — a request/response pair modeling an external API
//                         spec, each with a single (De)serialize derive.
//
// Run `cargo check --example <name>` to inspect each one in isolation.
